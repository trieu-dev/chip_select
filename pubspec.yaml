name: multi_select
description: >-
  A customizable multi-select Flutter widget with chip display and live search
  filtering. Integrates with Flutter's Form/FormField system for seamless
  validation.
version: 0.1.0
homepage: https://github.com/trieu-dev/multi_select
repository: https://github.com/trieu-dev/multi_select
issue_tracker: https://github.com/trieu-dev/multi_select/issues

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  # No assets or fonts required.
